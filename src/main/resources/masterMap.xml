<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
	"HTTP://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- These settings control SqlMapClient configuration details, primarily to do with transaction
	management. They are all optional (more detail later in this document).-->

	<settings>
		<setting name="defaultStatementTimeout" value="5000" />
		<setting name="mapUnderscoreToCamelCase" value="true"/>
 	</settings>

	<mappers>
		<mapper resource="sqlMap/mSequenceSql.xml" />
		<mapper resource="sqlMap/mAdminSql.xml" />
		<mapper resource="sqlMap/mLoginSql.xml" />
		<mapper resource="sqlMap/mMasterSql.xml" />
		<mapper resource="sqlMap/mUsersSql.xml" />
		<mapper resource="sqlMap/mAdminSql.xml" />
		<mapper resource="sqlMap/mNotiSql.xml" />
	</mappers>

</configuration>
